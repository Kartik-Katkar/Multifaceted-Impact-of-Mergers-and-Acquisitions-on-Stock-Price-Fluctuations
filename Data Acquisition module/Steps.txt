cd flask_app
python -m venv venv
source venv/bin/activate  # On Windows, use `venv\Scripts\activate`

pip install Flask PyPDF2 pandas beautifulsoup4 requests python-dotenv

create a .env file in the same directory and copy the envstructure.txt and place your contents in .env.

get the webscraperAPI key from https://www.scraperapi.com/ website (free trial) and replace YOUR_ACTUAL_SCRAPERAPI_KEY with your actual 
ScraperAPI key in .env file.

for video processing
pip install openai-whisper ffmpeg-python pytube torch
pip install numpy<2

must have ffmpeg installed https://www.youtube.com/watch?v=jZLqNocSQDM 

for image processing
pip install easyocr opencv-python

used multithreading for automating the scraping and appending process 

you can't keep sending and receiving the data from LLM API so train your own model to do so. 
Dataset Generated by me. trained an AI model to
prevent it. Worked on data cleaning Merged different Datasets.

For this Filter before entering the database the model training and code available 
at : https://github.com/Kartik-Katkar/Malicious-Prompt-Filter-for-RAG-Database/tree/main 