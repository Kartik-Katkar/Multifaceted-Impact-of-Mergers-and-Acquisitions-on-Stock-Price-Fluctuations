# Create a virtual environment named 'env' (or any name you prefer)
python -m venv env

# Activate the virtual environment on Windows
.\env\Scripts\activate

# Activate the virtual environment on macOS/Linux
source env/bin/activate

pip install nltk python-dotenv sentence-transformers numpy
pip install faiss-cpu flask


run the below python code
# Initialize NLTK resources
nltk.download('punkt')
nltk.download('stopwords')
nltk.download('wordnet')

started the LLM based response part

pip install -q -U google-generativeai
pip install python-dotenv
pip install pandas yfinance

url = f'https://financialmodelingprep.com/api/v3/ratios/{ticker}?apikey=yT72Tp1YrJehGBySNBrAgwc5WZ0ZnSif'